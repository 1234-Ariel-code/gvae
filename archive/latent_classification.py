#!/usr/bin/env python3 # -*- coding: utf-8 -*-
"""
Latent-space classification (NO pre-trained weight loading required). Idea: - If loading pre-trained weights keeps failing due to architecture mismatch, we *train the VAE weights ourselves* (on the same genotype matrix X), then extract latent features and train the classifier. - This guarantees perfect weight/model match because the weights are produced by the exact model code in this script. Supports: - baseline VAE (K=1 for feature extraction => use mu) - beta-VAE (K=1 for feature extraction => use mu, with beta in KL) - qgVAE (K=NS for feature extraction => sample K times, use quantile features) Notes: - Training uses a standard VAE objective: recon_loss + beta * KL - For qgVAE, training is still a standard VAE; qgVAE "magic" is in extraction: sample multiple times and aggregate with row-wise quantiles. Run (example): python3 latent_classification.py \ --disease T2D \ --base_path /work/long_lab/for_Ariel/files/pruned \ --model_type qgvae \ --latent_dim 100 \ --num_samples 150 \ --num_layers 4 \ --feature_mode gwas_top \ --downsample_d 50000 \ --assoc_path /work/long_lab/for_Ariel/gwas_results/T2D_gwas.assoc \ --tped_file /work/long_lab/for_Ariel/files/pruned/T2D_origin.tped \ --train_vae_epochs 50 \ --vae_batch_size 256 \ --batch_size 256 \ --epochs 120 \ --cache_latents \ --out_root /work/long_lab/Ariel_Kemogne/Representation_learning/AJHG/T2D/latent_classification_outputs \ --make_plots
"""
import os
import json
import pickle
import argparse from dataclasses
import dataclass from typing
import Optional, Tuple, List
import numpy as np
import pandas as pd
import polars as pl from sklearn.model_selection
import train_test_split from sklearn.metrics
import roc_auc_score, accuracy_score
import tensorflow as tf from tensorflow.keras
import layers from tensorflow.keras.metrics
import AUC
# ------------------------------ # Repro / TF setup
# ------------------------------
def set_seed(seed: int = 42): np.random.seed(seed) tf.random.set_seed(seed)
def enable_gpu_memory_growth(): gpus = tf.config.list_physical_devices("GPU") for g in gpus:
try: tf.config.experimental.set_memory_growth(g, True)
except Exception: pass
# ------------------------------ # SNP ID readers (for alignment checks / GWAS-top)
# ------------------------------
def _read_snp_ids_from_tped(tped_file: str) -> list[str]: snp_ids = [] with open(tped_file, "r") as f: for line in f: if not line.strip(): continue parts = line.split() snp_ids.append(parts[1]) return snp_ids
def _read_snp_ids_from_bim(bim_file: str) -> list[str]: snp_ids = [] with open(bim_file, "r") as f: for line in f: if not line.strip(): continue parts = line.split() snp_ids.append(parts[1]) return snp_ids
def _load_gwas_assoc(assoc_path: str) -> Tuple[np.ndarray, np.ndarray]: snps = [] ps = [] with open(assoc_path, "r") as f: header = f.readline().strip().split() if not header: raise ValueError(f"Empty GWAS file: {assoc_path}") if "SNP" not in header or "P" not in header: raise ValueError(f"GWAS file must contain SNP and P columns. Header={header[:30]}") snp_i = header.index("SNP") p_i = header.index("P") for line in f: if not line.strip(): continue parts = line.split() if len(parts) <= max(snp_i, p_i): continue snp = parts[snp_i] p = parts[p_i]
try: pv = float(p)
except Exception: continue snps.append(snp) ps.append(pv) if not snps: raise ValueError(f"No usable SNP/P rows found in: {assoc_path}") return np.array(snps, dtype=str), np.array(ps, dtype=float)
# ------------------------------ # Data loading
# ------------------------------
def load_genotype_csv( file_path: str, separator: str = ",", *, tped_file: Optional[str] = None, bim_file: Optional[str] = None, feature_mode: Optional[str] = None, # None | "random" | "gwas_top" downsample_d: Optional[int] = None, gwas_assoc_path: Optional[str] = None, downsample_n: Optional[int] = None, downsample_frac: Optional[float] = None, seed: int = 42, return_indices: bool = False, ) -> Tuple[np.ndarray, Optional[np.ndarray], Optional[np.ndarray], Optional[List[str]]]: df = pl.read_csv( file_path, has_header=False, separator=separator, null_values=["9", "NA", "NaN"], infer_schema_length=1000, ).with_columns(pl.all().cast(pl.Float32)) arr_noT = df.fill_null(strategy="mean").to_numpy() arr_T = df.fill_null(strategy="mean").transpose(include_header=False).to_numpy() chosen = None snp_ids = None if tped_file is not None: snp_ids = _read_snp_ids_from_tped(tped_file) M = len(snp_ids) if arr_noT.shape[1] == M: chosen = arr_noT
elif arr_T.shape[1] == M: chosen = arr_T
else: raise ValueError( f"[ORIENTATION ERROR] Neither orientation matches TPED SNP count.\n" f"TPED SNPs: {M}\n" f"arr_noT shape: {arr_noT.shape}\n" f"arr_T shape: {arr_T.shape}\n" )
else: chosen = arr_T arr = chosen.astype(np.float32, copy=False) N, D = arr.shape rng = np.random.default_rng(seed) row_idx = np.arange(N) col_idx = np.arange(D) kept_snp_ids = None # --- Row downsampling if (downsample_n is not None) and (downsample_frac is not None): raise ValueError("Use only one of downsample_n or downsample_frac.") if downsample_frac is not None: if not (0.0 < downsample_frac <= 1.0): raise ValueError("downsample_frac must be in (0,1].") target_n = max(1, int(round(N * downsample_frac)))
elif downsample_n is not None: if downsample_n <= 0: raise ValueError("downsample_n must be > 0.") target_n = min(N, int(downsample_n))
else: target_n = None if target_n is not None and target_n < N: row_idx = rng.choice(N, size=target_n, replace=False) row_idx.sort() arr = arr[row_idx, :] N = arr.shape[0] # --- Feature downsampling if feature_mode is None: pass
elif feature_mode == "random": if downsample_d is None: raise ValueError("feature_mode='random' requires downsample_d.") target_d = min(D, int(downsample_d)) if target_d < D: col_idx = rng.choice(D, size=target_d, replace=False) col_idx.sort() arr = arr[:, col_idx] if snp_ids is not None: kept_snp_ids = np.array(snp_ids, dtype=str)[col_idx].tolist()
elif feature_mode == "gwas_top": if downsample_d is None or gwas_assoc_path is None: raise ValueError("feature_mode='gwas_top' requires downsample_d and gwas_assoc_path.") if snp_ids is None: if tped_file is not None: snp_ids = _read_snp_ids_from_tped(tped_file)
elif bim_file is not None: snp_ids = _read_snp_ids_from_bim(bim_file)
else: raise ValueError("Need tped_file or bim_file for feature_mode='gwas_top'.") if len(snp_ids) != D: raise ValueError(f"SNP-ID count ({len(snp_ids)}) != D ({D}). CSV columns must align to TPED/BIM SNP IDs.") gwas_snps, gwas_p = _load_gwas_assoc(gwas_assoc_path) order = np.argsort(gwas_p) topM = int(min(len(order), downsample_d)) gwas_top_snps = set(gwas_snps[order[:topM]].tolist()) snp_ids_arr = np.array(snp_ids, dtype=str) mask = np.isin(snp_ids_arr, list(gwas_top_snps)) if mask.sum() == 0: raise RuntimeError("No overlap between GWAS top SNPs and genotype columns (SNP ID mismatch).") col_idx = np.where(mask)[0] arr = arr[:, col_idx] kept_snp_ids = snp_ids_arr[col_idx].tolist()
else: raise ValueError("feature_mode must be one of: None | 'random' | 'gwas_top'") arr = arr.astype(np.float32, copy=False) if return_indices: return arr, row_idx, col_idx, kept_snp_ids return arr, None, None, kept_snp_ids
def looad_phenotype(phen_path: str) -> Tuple[np.ndarray, np.ndarray]: df = pd.read_csv(phen_path, delim_whitespace=True, header=None) if df.shape[1] < 3: raise ValueError(f"Phenotype file must have >=3 columns (FID IID PHENO): {phen_path}") y = df.iloc[:, 2].to_numpy() keep = np.isin(y, [0, 1]) return y.astype(np.int32), keep
def load_phenotype(phen_path: str) -> Tuple[np.ndarray, np.ndarray]: df = pd.read_csv(phen_path, delim_whitespace=True, header=None) if df.shape[1] < 3: raise ValueError(f"Phenotype file must have >=3 columns (FID IID PHENO): {phen_path}") y_raw = df.iloc[:, 2].to_numpy()
# Keep WTCCC-style case/control coding: 1/2 keep = np.isin(y_raw, [1, 2])
# Map to 0/1 for keras y = np.full_like(y_raw, fill_value=-1, dtype=np.int32) y[y_raw == 1] = 0 y[y_raw == 2] = 1 return y, keep
def load_phenotype_auto(phen_path: str):
"""
Returns: y (np.ndarray): labels (0/1 for binary; float for quantitative) keep (np.ndarray bool): which rows to keep (exclude missing codes) task (str): "classification" or "regression" info (dict): quick summary
"""
df = pd.read_csv(phen_path, delim_whitespace=True, header=None) if df.shape[1] < 3: raise ValueError(f"Phenotype file must have >=3 columns (FID IID PHENO): {phen_path}") y_raw = df.iloc[:, 2].to_numpy()
# Common missing codes in genetics missing_codes = set([-9, 9, 99, 999]) keep = np.ones_like(y_raw, dtype=bool)
# mark NaNs if np.issubdtype(y_raw.dtype, np.number): keep &= ~np.isnan(y_raw.astype(float, copy=False))
else: # try coerce; anything failing becomes NaN y_tmp = pd.to_numeric(pd.Series(y_raw), errors="coerce").to_numpy() keep &= ~np.isnan(y_tmp) y_raw = y_tmp # drop missing codes keep &= ~np.isin(y_raw, list(missing_codes)) y_kept = y_raw[keep] uniq = np.unique(y_kept)
# --- Binary classification cases if set(uniq.tolist()).issubset({0, 1}): task = "classification" y = y_raw.astype(np.int32)
elif set(uniq.tolist()).issubset({1, 2}): task = "classification" # WTCCC-style mapping: 1->0 (control), 2->1 (case) y = np.full_like(y_raw, -1, dtype=np.int32) y[y_raw == 1] = 0 y[y_raw == 2] = 1 keep &= (y != -1)
else: # --- Quantitative trait => regression task = "regression" y = y_raw.astype(np.float32) info = { "n_total": int(len(y_raw)), "n_kept": int(keep.sum()), "n_unique_kept": int(len(np.unique(y_raw[keep]))), "min_kept": float(np.min(y_raw[keep])), "max_kept": float(np.max(y_raw[keep])), "task": task, } return y, keep, task, info
# ------------------------------ # VAE (we learn weights here)
# ------------------------------
def compute_initial_neurons(num_layers: int, latent_dim: int) -> int: return latent_dim * (2 ** (num_layers - 1))
def build_encoder(original_dim: int, latent_dim: int, num_layers: int, *, use_layernorm: bool = False) -> tf.keras.Model: init = compute_initial_neurons(num_layers, latent_dim) inp = layers.Input(shape=(original_dim,), name="x") x = inp if use_layernorm: x = layers.LayerNormalization(name="enc_ln")(x) n = init for i in range(num_layers): x = layers.Dense(n, activation="relu", name=f"enc_dense_{i}")(x) n //= 2 z_params = layers.Dense(latent_dim * 2, name="z_params")(x)
# mu, log_var return tf.keras.Model(inp, z_params, name="encoder")
def build_decoder(original_dim: int, latent_dim: int, num_layers: int) -> tf.keras.Model: # mirror of encoder widths init = compute_initial_neurons(num_layers, latent_dim) widths = [] n = init for _ in range(num_layers): widths.append(n) n //= 2 inp = layers.Input(shape=(latent_dim,), name="z") x = inp # reverse widths for i, w in enumerate(reversed(widths)): x = layers.Dense(w, activation="relu", name=f"dec_dense_{i}")(x)
# genotype reconstruction (continuous) out = layers.Dense(original_dim, activation="linear", name="x_hat")(x) return tf.keras.Model(inp, out, name="decoder")
class VAE(tf.keras.Model):
def __init__(self, encoder: tf.keras.Model, decoder: tf.keras.Model, beta: float = 1.0): super().__init__() self.encoder = encoder self.decoder = decoder self.beta = float(beta) self.loss_tracker = tf.keras.metrics.Mean(name="loss") self.recon_tracker = tf.keras.metrics.Mean(name="recon") self.kl_tracker = tf.keras.metrics.Mean(name="kl") @property
def metrics(self): return [self.loss_tracker, self.recon_tracker, self.kl_tracker]
def encode(self, x): z_params = self.encoder(x, training=False) mu, log_var = tf.split(z_params, num_or_size_splits=2, axis=1) return mu, log_var
def sample(self, mu, log_var): eps = tf.random.normal(tf.shape(mu), dtype=tf.float32) return mu + eps * tf.exp(0.5 * log_var)
def train_step(self, data): x = data[0] if isinstance(data, (tuple, list)) else data x = tf.cast(x, tf.float32) with tf.GradientTape() as tape: z_params = self.encoder(x, training=True) mu, log_var = tf.split(z_params, 2, axis=1) z = self.sample(mu, log_var) x_hat = self.decoder(z, training=True)
# Recon loss: MSE per sample recon = tf.reduce_mean(tf.reduce_sum(tf.square(x - x_hat), axis=1))
# KL: sum over latent dims, mean over batch kl = -0.5 * tf.reduce_mean(tf.reduce_sum(1.0 + log_var - tf.square(mu) - tf.exp(log_var), axis=1)) loss = recon + self.beta * kl grads = tape.gradient(loss, self.trainable_variables) self.optimizer.apply_gradients(zip(grads, self.trainable_variables)) self.loss_tracker.update_state(loss) self.recon_tracker.update_state(recon) self.kl_tracker.update_state(kl) return {"loss": self.loss_tracker.result(), "recon": self.recon_tracker.result(), "kl": self.kl_tracker.result()}
def train_vae_on_X( X: np.ndarray, *, latent_dim: int, num_layers: int, beta: float, use_layernorm: bool, epochs: int, batch_size: int, lr: float, seed: int, verbose: int = 2, ) -> VAE: tf.keras.utils.set_random_seed(seed) encoder = build_encoder(X.shape[1], latent_dim, num_layers, use_layernorm=use_layernorm) decoder = build_decoder(X.shape[1], latent_dim, num_layers) vae = VAE(encoder, decoder, beta=beta) vae.compile(optimizer=tf.keras.optimizers.Adam(lr)) ds = tf.data.Dataset.from_tensor_slices(X.astype(np.float32)) ds = ds.shuffle(min(len(X), 10000), seed=seed, reshuffle_each_iteration=True) ds = ds.batch(batch_size).prefetch(tf.data.AUTOTUNE) vae.fit(ds, epochs=epochs, verbose=verbose) return vae
# ------------------------------ # Latent feature extraction (from learned VAE)
# ------------------------------ @dataclass
class LatentConfig: disease: str model_type: str # "baseline" | "qgvae" | "betavae" latent_dim: int num_samples: int # NS (K) used in extraction for qgvae num_layers: int # L beta: Optional[float] # only for betavae
def rowwise_quantiles_from_zstack(z_stack: tf.Tensor) -> tf.Tensor:
"""
z_stack: (K, B, LD) Returns: concat(Q25, Q75) => (B, 2*LD)
"""
sorted_samples = tf.sort(z_stack, axis=0) K_ = tf.shape(sorted_samples)[0] idx_25 = tf.cast(0.25 * tf.cast(K_, tf.float32), tf.int32) idx_75 = tf.cast(0.75 * tf.cast(K_, tf.float32), tf.int32) q25 = tf.gather(sorted_samples, idx_25, axis=0) q75 = tf.gather(sorted_samples, idx_75, axis=0) return tf.concat([q25, q75], axis=-1)
def extract_latent_features_from_vae(X: np.ndarray, cfg: LatentConfig, vae: VAE, batch_size: int) -> np.ndarray: feats = [] for i in range(0, X.shape[0], batch_size): xb = tf.convert_to_tensor(X[i:i+batch_size], dtype=tf.float32) mu, log_var = vae.encode(xb) if cfg.model_type in ("baseline", "betavae"): fb = mu # (B, LD)
else: K = int(max(1, cfg.num_samples)) z_list = [] for _ in range(K): eps = tf.random.normal(shape=tf.shape(mu), dtype=tf.float32) z = mu + eps * tf.exp(0.5 * log_var) z_list.append(z) z_stack = tf.stack(z_list, axis=0)
# (K, B, LD) fb = rowwise_quantiles_from_zstack(z_stack)
# (B, 2*LD) feats.append(fb.numpy()) return np.vstack(feats)
# ------------------------------ # Classifier
# ------------------------------
def build_classifier(input_dim: int, seed: int = 42) -> tf.keras.Model: tf.keras.utils.set_random_seed(seed) inp = layers.Input(shape=(input_dim,)) x = layers.Dense(256, activation="relu")(inp) x = layers.Dropout(0.25)(x) x = layers.Dense(128, activation="relu")(x) x = layers.Dropout(0.25)(x) x = layers.Dense(64, activation="relu")(x) out = layers.Dense(1, activation="sigmoid")(x) model = tf.keras.Model(inp, out) model.compile( optimizer=tf.keras.optimizers.Adam(1e-3), loss="binary_crossentropy", metrics=["accuracy", AUC(name="auc")], ) return model
def train_classifier(Z: np.ndarray, y: np.ndarray, *, seed: int, epochs: int, batch_size: int, val_size: float): Xtr, Xva, ytr, yva = train_test_split( Z, y, test_size=val_size, random_state=seed, stratify=y ) clf = build_classifier(input_dim=Z.shape[1], seed=seed) callbacks = [ tf.keras.callbacks.EarlyStopping(monitor="val_auc", mode="max", patience=10, restore_best_weights=True), tf.keras.callbacks.ReduceLROnPlateau(monitor="val_auc", mode="max", factor=0.5, patience=5, min_lr=1e-6), ] hist = clf.fit( Xtr, ytr, validation_data=(Xva, yva), epochs=epochs, batch_size=batch_size, verbose=2, callbacks=callbacks ) pva = clf.predict(Xva, batch_size=batch_size, verbose=0).reshape(-1) auc = roc_auc_score(yva, pva) if len(np.unique(yva)) == 2 else float("nan") acc = accuracy_score(yva, (pva >= 0.5).astype(np.int32)) return hist, {"val_auc_final": float(auc), "val_acc_final": float(acc), "n_val": int(len(yva))}
def build_regressor(input_dim: int, seed: int = 42) -> tf.keras.Model: tf.keras.utils.set_random_seed(seed) inp = layers.Input(shape=(input_dim,)) x = layers.Dense(256, activation="relu")(inp) x = layers.Dropout(0.25)(x) x = layers.Dense(128, activation="relu")(x) x = layers.Dropout(0.25)(x) x = layers.Dense(64, activation="relu")(x) out = layers.Dense(1, activation="linear")(x) model = tf.keras.Model(inp, out) model.compile( optimizer=tf.keras.optimizers.Adam(1e-3), loss="mse", metrics=[ tf.keras.metrics.MeanAbsoluteError(name="mae"), tf.keras.metrics.RootMeanSquaredError(name="rmse"), ], ) return model
def train_regressor(Z: np.ndarray, y: np.ndarray, *, seed: int, epochs: int, batch_size: int, val_size: float): Xtr, Xva, ytr, yva = train_test_split( Z, y, test_size=val_size, random_state=seed )
# (Optional but recommended) standardize target for stable training y_mean = float(np.mean(ytr)) y_std = float(np.std(ytr) + 1e-8) ytr_s = (ytr - y_mean) / y_std yva_s = (yva - y_mean) / y_std reg = build_regressor(input_dim=Z.shape[1], seed=seed) callbacks = [ tf.keras.callbacks.EarlyStopping(monitor="val_rmse", mode="min", patience=10, restore_best_weights=True), tf.keras.callbacks.ReduceLROnPlateau(monitor="val_rmse", mode="min", factor=0.5, patience=5, min_lr=1e-6), ] hist = reg.fit( Xtr, ytr_s, validation_data=(Xva, yva_s), epochs=epochs, batch_size=batch_size, verbose=2, callbacks=callbacks ) pva_s = reg.predict(Xva, batch_size=batch_size, verbose=0).reshape(-1) pva = pva_s * y_std + y_mean # back to original scale # metrics on original scale mse = float(np.mean((yva - pva) ** 2)) rmse = float(np.sqrt(mse)) mae = float(np.mean(np.abs(yva - pva)))
# R^2 ss_res = float(np.sum((yva - pva) ** 2)) ss_tot = float(np.sum((yva - np.mean(yva)) ** 2) + 1e-12) r2 = float(1.0 - ss_res / ss_tot)
# Pearson corr (optional but useful) if np.std(pva) > 0 and np.std(yva) > 0: corr = float(np.corrcoef(yva, pva)[0, 1])
else: corr = float("nan")
# Save these into history too (so your plotting can use them) hist.history.setdefault("val_r2", []) hist.history.setdefault("val_corr", []) hist.history["val_r2"].append(r2) hist.history["val_corr"].append(corr) return hist, { "val_r2_final": r2, "val_rmse_final": rmse, "val_mae_final": mae, "val_corr_final": corr, "n_val": int(len(yva)), "y_mean_train": y_mean, "y_std_train": y_std, }
# ------------------------------ # Plotting
# ------------------------------
def plot_disease_curves(history_dir: str, out_dir: str, disease: str):
import matplotlib.pyplot as plt os.makedirs(out_dir, exist_ok=True) records = [] for fn in os.listdir(history_dir): if not fn.endswith(".pickle"): continue if not fn.startswith(f"{disease}_"): continue with open(os.path.join(history_dir, fn), "rb") as f: h = pickle.load(f)
# {disease}_{model}_LD{LD}_NS{NS}_L{L}_B{betaTag}.pickle stem = fn[:-7] parts = stem.split("_") model = parts[1] LD = int(parts[2].replace("LD", "")) NS = int(parts[3].replace("NS", "")) L = int(parts[4].replace("L", "")) beta = parts[5].replace("B", "") if len(parts) >= 6 else "NA" records.append({"model": model, "LD": LD, "NS": NS, "L": L, "beta": beta, "history": h}) if not records: print(f"[plot] No histories found for {disease} in {history_dir}") return groups = {} for r in records: key = (r["LD"], r["L"]) groups.setdefault(key, []).append(r) keys = sorted(groups.keys(), key=lambda x: x[0]) n = len(keys) ncols = min(3, n) nrows = int(np.ceil(n / ncols))
def line_label(r): if r["model"] == "baseline": return "baseline K=1" if r["model"] == "qgvae": return f"qgVAE K={r['NS']}" if r["model"] == "betavae": return f"betaVAE β={r['beta']} K=1" return r["model"]
def sort_key(r): if r["model"] == "baseline": return (0, 0, 0) if r["model"] == "qgvae": return (1, r["NS"], 0) if r["model"] == "betavae": return (2, 0, r["beta"]) return (9, 0, 0)
def _plot(metric_key: str, out_name: str, title_suffix: str): fig, axes = plt.subplots(nrows, ncols, figsize=(6*ncols, 4.5*nrows), sharey=False) if nrows == 1 and ncols == 1: axes = np.array([[axes]])
elif nrows == 1: axes = np.array([axes])
elif ncols == 1: axes = np.array([[ax] for ax in axes]) for idx, key in enumerate(keys): r0, c0 = divmod(idx, ncols) ax = axes[r0, c0] LD, L = key rows = sorted(groups[key], key=sort_key) for r in rows: hist = r["history"] if metric_key in hist: ax.plot(hist[metric_key], label=line_label(r)) ax.set_title(f"{disease} | LD={LD}, L={L} ({title_suffix})") ax.set_xlabel("Epoch") ax.set_ylabel(metric_key.replace("_", " ")) ax.grid(True, alpha=0.3) ax.legend(loc="lower right", frameon=True) for idx in range(n, nrows*ncols): r0, c0 = divmod(idx, ncols) axes[r0, c0].axis("off") plt.tight_layout() fig.savefig(os.path.join(out_dir, out_name), dpi=300, bbox_inches="tight") plt.close(fig) _plot("val_accuracy", f"{disease}_val_accuracy.png", "Validation Accuracy") _plot("val_auc", f"{disease}_val_auc.png", "Validation AUC")
def pllot_per_config_models(history_dir: str, out_dir: str, disease: str, choose_beta: str = "best_auc"):
"""
One figure per config (LD, NS, L) comparing baseline vs qgvae vs betavae. Saves TWO figs per config: val_accuracy and val_auc. choose_beta: - "best_auc": choose betaVAE beta that maximizes peak val_auc - "beta_1.0": use beta=1.0 only (if present)
"""
import os
import numpy as np
import pickle
import matplotlib.pyplot as plt os.makedirs(out_dir, exist_ok=True)
# ------------------------- # 1) Load all histories # ------------------------- runs = [] for fn in os.listdir(history_dir): if not (fn.endswith(".pickle") and fn.startswith(f"{disease}_")): continue # {disease}_{model}_LD{LD}_NS{NS}_L{L}_B{betaTag}.pickle stem = fn[:-7] parts = stem.split("_") if len(parts) < 6: continue model = parts[1] LD = int(parts[2].replace("LD", "")) NS = int(parts[3].replace("NS", "")) L = int(parts[4].replace("L", "")) beta = parts[5].replace("B", "")
# "NA" or float string with open(os.path.join(history_dir, fn), "rb") as f: hist = pickle.load(f)
# ensure keys exist if "val_accuracy" not in hist: hist["val_accuracy"] = [] if "val_auc" not in hist: hist["val_auc"] = [] runs.append({ "fn": fn, "model": model, "LD": LD, "NS": NS, "L": L, "beta": beta, "hist": hist }) if not runs: print(f"[plot] No histories found for {disease} in {history_dir}") return # Helper: peak metric
def peak(hist_list): if hist_list is None or len(hist_list) == 0: return float("-inf") return float(np.nanmax(np.array(hist_list, dtype=float)))
# ------------------------- # 2) Index runs for lookup # ------------------------- # baseline indexed by (LD,L) baseline_map = {} # qgvae indexed by (LD,NS,L) qg_map = {} # betavae indexed by (LD,L) -> list of (beta, run) beta_map = {} for r in runs: if r["model"] == "baseline": baseline_map[(r["LD"], r["L"])] = r
elif r["model"] == "qgvae": qg_map[(r["LD"], r["NS"], r["L"])] = r
elif r["model"] == "betavae": beta_map.setdefault((r["LD"], r["L"]), []).append(r)
# all configs come from qgvae configs (LD,NS,L); you can extend if needed all_configs = sorted(qg_map.keys(), key=lambda x: (x[0], x[2], x[1]))
# (LD,NS,L)
# ------------------------- # 3) Choose betaVAE run per (LD,L)
# -------------------------
def pick_beta_run(LD, L): lst = beta_map.get((LD, L), []) if not lst: return None if choose_beta == "beta_1.0": for r in lst:
try: if abs(float(r["beta"]) - 1.0) < 1e-9: return r
except Exception: pass return None # not found # default: best by peak val_auc best = None best_score = float("-inf") for r in lst: score = peak(r["hist"].get("val_auc", [])) if score > best_score: best_score = score best = r return best # ------------------------- # 4) Plot per config # -------------------------
def plot_metric(metric_key: str, cfg_key, baseline_run, qg_run, beta_run): LD, NS, L = cfg_key fig = plt.figure(figsize=(7.5, 5.0)) ax = plt.gca()
# baseline if baseline_run is not None and len(baseline_run["hist"].get(metric_key, [])) > 0: ax.plot(baseline_run["hist"][metric_key], label="baseline (K=1)")
# qgvae if qg_run is not None and len(qg_run["hist"].get(metric_key, [])) > 0: ax.plot(qg_run["hist"][metric_key], label=f"qgVAE (K={NS})")
# betavae if beta_run is not None and len(beta_run["hist"].get(metric_key, [])) > 0: ax.plot(beta_run["hist"][metric_key], label=f"betaVAE (β={beta_run['beta']})") ax.set_title(f"{disease} | LD={LD}, NS={NS}, L={L} ({metric_key.replace('_',' ')})") ax.set_xlabel("Epoch") ax.set_ylabel(metric_key.replace("_", " ")) ax.grid(True, alpha=0.3) ax.legend(loc="best", frameon=True) plt.tight_layout() return fig # Output subfolders (clean) acc_dir = os.path.join(out_dir, "per_config_accuracy") auc_dir = os.path.join(out_dir, "per_config_auc") os.makedirs(acc_dir, exist_ok=True) os.makedirs(auc_dir, exist_ok=True) for cfg in all_configs: LD, NS, L = cfg qg_run = qg_map.get(cfg, None) baseline_run = baseline_map.get((LD, L), None) beta_run = pick_beta_run(LD, L)
# Skip if qg run missing (shouldn't happen) if qg_run is None: continue # Accuracy fig fig1 = plot_metric("val_accuracy", cfg, baseline_run, qg_run, beta_run) out1 = os.path.join(acc_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_accuracy.png") fig1.savefig(out1, dpi=300, bbox_inches="tight") plt.close(fig1)
# AUC fig fig2 = plot_metric("val_auc", cfg, baseline_run, qg_run, beta_run) out2 = os.path.join(auc_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_auc.png") fig2.savefig(out2, dpi=300, bbox_inches="tight") plt.close(fig2) print(f"[OK] Saved per-config figs in:\n {acc_dir}\n {auc_dir}")
def plot_per_config_models(history_dir: str, out_dir: str, disease: str, choose_beta: str = "auto"):
"""
One figure per config (LD, NS, L) comparing baseline vs qgvae vs betavae. Classification: - produces per-config plots for val_accuracy and val_auc - betaVAE selection by best peak val_auc (choose_beta="best_auc") Regression: - produces per-config plots for val_rmse, val_mae, val_r2 - betaVAE selection by best (lowest) min val_rmse (choose_beta="best_rmse") choose_beta: - "auto": classification -> best_auc, regression -> best_rmse - "best_auc" - "best_rmse" - "beta_1.0" (use beta=1.0 if present)
"""
import os
import numpy as np
import pickle
import matplotlib.pyplot as plt os.makedirs(out_dir, exist_ok=True)
# ------------------------- # 1) Load all histories # ------------------------- runs = [] for fn in os.listdir(history_dir): if not (fn.endswith(".pickle") and fn.startswith(f"{disease}_")): continue # {disease}_{model}_LD{LD}_NS{NS}_L{L}_B{betaTag}.pickle stem = fn[:-7] parts = stem.split("_") if len(parts) < 6: continue model = parts[1] LD = int(parts[2].replace("LD", "")) NS = int(parts[3].replace("NS", "")) L = int(parts[4].replace("L", "")) beta = parts[5].replace("B", "")
# "NA" or float string with open(os.path.join(history_dir, fn), "rb") as f: hist = pickle.load(f)
# normalize missing keys for k in ["val_accuracy", "val_auc", "val_rmse", "val_mae", "val_r2"]: hist.setdefault(k, []) runs.append({ "fn": fn, "model": model, "LD": LD, "NS": NS, "L": L, "beta": beta, "hist": hist }) if not runs: print(f"[plot] No histories found for {disease} in {history_dir}") return # ------------------------- # 2) Detect task type # ------------------------- # If any run has regression metric values, treat as regression has_reg = any((len(r["hist"].get("val_rmse", [])) > 0) for r in runs) task = "regression" if has_reg else "classification" if choose_beta == "auto": choose_beta = "best_rmse" if task == "regression" else "best_auc" # ------------------------- # Helpers: peak/min metric # -------------------------
def peak(vals): if vals is None or len(vals) == 0: return float("-inf") return float(np.nanmax(np.array(vals, dtype=float)))
def minval(vals): if vals is None or len(vals) == 0: return float("inf") return float(np.nanmin(np.array(vals, dtype=float)))
# ------------------------- # 3) Index runs for lookup # ------------------------- baseline_map = {} # (LD, L) -> run qg_map = {} # (LD, NS, L) -> run beta_map = {} # (LD, L) -> list of runs for r in runs: if r["model"] == "baseline": baseline_map[(r["LD"], r["L"])] = r
elif r["model"] == "qgvae": qg_map[(r["LD"], r["NS"], r["L"])] = r
elif r["model"] == "betavae": beta_map.setdefault((r["LD"], r["L"]), []).append(r)
# configs come from qgvae configs all_configs = sorted(qg_map.keys(), key=lambda x: (x[0], x[2], x[1]))
# (LD,NS,L)
# ------------------------- # 4) Pick betaVAE per (LD,L)
# -------------------------
def pick_beta_run(LD, L): lst = beta_map.get((LD, L), []) if not lst: return None if choose_beta == "beta_1.0": for r in lst:
try: if abs(float(r["beta"]) - 1.0) < 1e-9: return r
except Exception: pass return None if choose_beta == "best_auc": best = None best_score = float("-inf") for r in lst: score = peak(r["hist"].get("val_auc", [])) if score > best_score: best_score = score best = r return best if choose_beta == "best_rmse": best = None best_score = float("inf") for r in lst: score = minval(r["hist"].get("val_rmse", []))
# lower better if score < best_score: best_score = score best = r return best # fallback return lst[0] # ------------------------- # 5) Plot per config # -------------------------
def plot_metric(metric_key: str, cfg_key, baseline_run, qg_run, beta_run): LD, NS, L = cfg_key fig = plt.figure(figsize=(7.5, 5.0)) ax = plt.gca()
def safe_plot(run, label): vals = run["hist"].get(metric_key, []) if run is not None else [] if vals is not None and len(vals) > 0: ax.plot(vals, label=label) safe_plot(baseline_run, "baseline (K=1)") safe_plot(qg_run, f"qgVAE (K={NS})") if beta_run is not None: safe_plot(beta_run, f"betaVAE (β={beta_run['beta']})") ax.set_title(f"{disease} | LD={LD}, NS={NS}, L={L} ({metric_key.replace('_',' ')})") ax.set_xlabel("Epoch") ax.set_ylabel(metric_key.replace("_", " ")) ax.grid(True, alpha=0.3) ax.legend(loc="best", frameon=True) plt.tight_layout() return fig # output subfolders if task == "classification": acc_dir = os.path.join(out_dir, "per_config_accuracy") auc_dir = os.path.join(out_dir, "per_config_auc") os.makedirs(acc_dir, exist_ok=True) os.makedirs(auc_dir, exist_ok=True) for cfg in all_configs: LD, NS, L = cfg qg_run = qg_map.get(cfg, None) baseline_run = baseline_map.get((LD, L), None) beta_run = pick_beta_run(LD, L) if qg_run is None: continue fig1 = plot_metric("val_accuracy", cfg, baseline_run, qg_run, beta_run) fig1.savefig(os.path.join(acc_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_accuracy.png"), dpi=300, bbox_inches="tight") plt.close(fig1) fig2 = plot_metric("val_auc", cfg, baseline_run, qg_run, beta_run) fig2.savefig(os.path.join(auc_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_auc.png"), dpi=300, bbox_inches="tight") plt.close(fig2) print(f"[OK] Saved per-config CLASSIFICATION figs in:\n {acc_dir}\n {auc_dir}")
else: rmse_dir = os.path.join(out_dir, "per_config_rmse") mae_dir = os.path.join(out_dir, "per_config_mae") r2_dir = os.path.join(out_dir, "per_config_r2") os.makedirs(rmse_dir, exist_ok=True) os.makedirs(mae_dir, exist_ok=True) os.makedirs(r2_dir, exist_ok=True) for cfg in all_configs: LD, NS, L = cfg qg_run = qg_map.get(cfg, None) baseline_run = baseline_map.get((LD, L), None) beta_run = pick_beta_run(LD, L) if qg_run is None: continue fig1 = plot_metric("val_rmse", cfg, baseline_run, qg_run, beta_run) fig1.savefig(os.path.join(rmse_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_rmse.png"), dpi=300, bbox_inches="tight") plt.close(fig1) fig2 = plot_metric("val_mae", cfg, baseline_run, qg_run, beta_run) fig2.savefig(os.path.join(mae_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_mae.png"), dpi=300, bbox_inches="tight") plt.close(fig2) fig3 = plot_metric("val_r2", cfg, baseline_run, qg_run, beta_run) fig3.savefig(os.path.join(r2_dir, f"{disease}_LD{LD}_NS{NS}_L{L}_val_r2.png"), dpi=300, bbox_inches="tight") plt.close(fig3) print(f"[OK] Saved per-config REGRESSION figs in:\n {rmse_dir}\n {mae_dir}\n {r2_dir}")
# ------------------------------ # Main
# ------------------------------
def main(): ap = argparse.ArgumentParser() ap.add_argument("--disease", required=True, type=str) ap.add_argument("--base_path", default="/work/long_lab/for_Ariel/files", type=str) ap.add_argument("--model_type", required=True, choices=["baseline", "qgvae", "betavae"]) ap.add_argument("--latent_dim", required=True, type=int) ap.add_argument("--num_samples", required=True, type=int) ap.add_argument("--num_layers", required=True, type=int) ap.add_argument("--beta", default=None, type=float)
# VAE training knobs (NEW) ap.add_argument("--train_vae_epochs", default=50, type=int) ap.add_argument("--vae_batch_size", default=256, type=int) ap.add_argument("--vae_lr", default=1e-3, type=float) ap.add_argument("--use_layernorm", action="store_true") ap.add_argument("--feature_mode", default="gwas_top", choices=["none", "random", "gwas_top"]) ap.add_argument("--downsample_d", default=50000, type=int) ap.add_argument("--assoc_path", default=None, type=str) ap.add_argument("--tped_file", default=None, type=str) ap.add_argument("--epochs", default=120, type=int) ap.add_argument("--batch_size", default=256, type=int) ap.add_argument("--val_size", default=0.2, type=float) ap.add_argument("--seed", default=42, type=int) ap.add_argument("--cache_latents", action="store_true") ap.add_argument("--out_root", default="latent_classification_outputs", type=str) ap.add_argument("--make_plots", action="store_true") args = ap.parse_args() set_seed(args.seed) enable_gpu_memory_growth() disease = args.disease base_path = args.base_path geno_csv = os.path.join(base_path, f"{disease}_filtered.csv") phen_path = os.path.join(base_path, f"{disease}_origin.phen") if args.tped_file is None: args.tped_file = os.path.join(base_path, f"{disease}_origin.tped") if args.assoc_path is None: args.assoc_path = f"/work/long_lab/for_Ariel/gwas_results/{disease}_gwas.assoc" fm = None if args.feature_mode == "none" else args.feature_mode X, _, _, _ = load_genotype_csv( geno_csv, separator=",", tped_file=args.tped_file, feature_mode=fm, downsample_d=args.downsample_d if fm is not None else None, gwas_assoc_path=args.assoc_path if fm == "gwas_top" else None, seed=args.seed, return_indices=False, ) #y_all, keep = load_phenotype(phen_path) y_all, keep, task, phen_info = load_phenotype_auto(phen_path) print("[PHENOTYPE]", phen_info) if keep.shape[0] != X.shape[0]: raise ValueError( f"Phenotype length ({keep.shape[0]}) != Genotype N ({X.shape[0]}).\n" f"Make sure CSV individuals align with .phen order." ) X = X[keep] y = y_all[keep] if args.model_type == "betavae" and args.beta is None: raise ValueError("--beta is required when --model_type=betavae") cfg = LatentConfig( disease=disease, model_type=args.model_type, latent_dim=args.latent_dim, num_samples=args.num_samples, num_layers=args.num_layers, beta=args.beta if args.model_type == "betavae" else None, ) out_root = args.out_root hist_dir = os.path.join(out_root, "class_history") met_dir = os.path.join(out_root, "class_metrics") lat_dir = os.path.join(out_root, "class_latents") sum_dir = os.path.join(out_root, "class_summary") fig_dir = os.path.join(out_root, "figures") for d in [hist_dir, met_dir, lat_dir, sum_dir, fig_dir]: os.makedirs(d, exist_ok=True) beta_tag = "NA" if cfg.beta is None else str(cfg.beta) run_tag = f"{disease}_{cfg.model_type}_LD{cfg.latent_dim}_NS{cfg.num_samples}_L{cfg.num_layers}_B{beta_tag}" cache_path = os.path.join(lat_dir, f"{run_tag}.npy")
# -------------------------- # 1) Train VAE weights here # -------------------------- beta_for_training = 1.0 if cfg.model_type == "betavae": beta_for_training = float(cfg.beta)
# baseline and qgvae both use beta=1.0 in training objective here. print("\n[STEP] Training VAE weights (in-script) so architecture/weights always match...") vae = train_vae_on_X( X=X, latent_dim=cfg.latent_dim, num_layers=cfg.num_layers, beta=beta_for_training, use_layernorm=bool(args.use_layernorm), epochs=int(args.train_vae_epochs), batch_size=int(args.vae_batch_size), lr=float(args.vae_lr), seed=int(args.seed), verbose=2, )
# -------------------------- # 2) Extract latents # -------------------------- if args.cache_latents and os.path.exists(cache_path): Z = np.load(cache_path) print(f"[OK] Loaded cached latents: {cache_path} shape={Z.shape}")
else: print("[STEP] Extracting latent features...") Z = extract_latent_features_from_vae(X=X, cfg=cfg, vae=vae, batch_size=args.batch_size) if args.cache_latents: np.save(cache_path, Z) print(f"[OK] Saved cached latents: {cache_path} shape={Z.shape}")
# -------------------------- # 3) Train classifier # -------------------------- if task == "classification": hist, final_metrics = train_classifier( Z=Z, y=y, seed=args.seed, epochs=args.epochs, batch_size=args.batch_size, val_size=args.val_size )
# ensure keys exist for plotting history_dict = hist.history history_dict.setdefault("val_auc", []) history_dict.setdefault("val_accuracy", [])
else: hist, final_metrics = train_regressor( Z=Z, y=y.astype(np.float32), seed=args.seed, epochs=args.epochs, batch_size=args.batch_size, val_size=args.val_size ) history_dict = hist.history # for plotting history_dict.setdefault("val_rmse", []) history_dict.setdefault("val_mae", []) history_dict.setdefault("val_loss", []) history_dict.setdefault("val_r2", []) history_dict.setdefault("val_corr", []) #hist, final_metrics = train_classifier( # Z=Z, y=y, seed=args.seed, epochs=args.epochs, batch_size=args.batch_size, val_size=args.val_size #) #history_dict = hist.history #if "val_auc" not in history_dict: # history_dict["val_auc"] = [] #if "val_accuracy" not in history_dict: # history_dict["val_accuracy"] = [] hist_path = os.path.join(hist_dir, f"{run_tag}.pickle") with open(hist_path, "wb") as f: pickle.dump(history_dict, f) met_path = os.path.join(met_dir, f"{run_tag}.json") with open(met_path, "w") as f: json.dump( { "run": run_tag, "disease": disease, "model_type": cfg.model_type, "LD": cfg.latent_dim, "NS": cfg.num_samples, "L": cfg.num_layers, "beta": cfg.beta, "Z_shape": list(Z.shape), "final": final_metrics, "vae_training": { "epochs": int(args.train_vae_epochs), "batch_size": int(args.vae_batch_size), "lr": float(args.vae_lr), "use_layernorm": bool(args.use_layernorm), "beta_in_objective": float(beta_for_training), }, "latent_cache": cache_path if args.cache_latents else "", }, f, indent=2, ) summary_path = os.path.join(sum_dir, f"{disease}_summary.csv") row = { "run": run_tag, "disease": disease, "model_type": cfg.model_type, "LD": cfg.latent_dim, "NS": cfg.num_samples, "L": cfg.num_layers, "beta": cfg.beta if cfg.beta is not None else np.nan, "val_auc_final": final_metrics["val_auc_final"], "val_acc_final": final_metrics["val_acc_final"], "n_val": final_metrics["n_val"], "Z_dim": Z.shape[1], "history_path": hist_path, "metrics_path": met_path, "latent_cache": cache_path if args.cache_latents else "", "vae_epochs": int(args.train_vae_epochs), "vae_batch_size": int(args.vae_batch_size), "vae_lr": float(args.vae_lr), "vae_beta": float(beta_for_training), "vae_use_layernorm": bool(args.use_layernorm), } if os.path.exists(summary_path): sdf = pd.read_csv(summary_path) sdf = sdf[sdf["run"] != run_tag] sdf = pd.concat([sdf, pd.DataFrame([row])], ignore_index=True)
else: sdf = pd.DataFrame([row]) sdf.to_csv(summary_path, index=False) print(f"\n[OK] Saved history : {hist_path}") print(f"[OK] Saved metrics : {met_path}") print(f"[OK] Updated summary: {summary_path}") if args.make_plots: #plot_disease_curves(history_dir=hist_dir, out_dir=fig_dir, disease=disease) plot_per_config_models(history_dir=hist_dir, out_dir=fig_dir, disease=disease, choose_beta="auto") print(f"[OK] Saved figures in: {fig_dir}")
if __name__ == "__main__": main()
